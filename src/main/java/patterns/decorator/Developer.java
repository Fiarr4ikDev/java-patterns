package patterns.decorator;

    /**
     * Интерфейс разработчика с методом {@link Developer#makeJob()} для описания его работы.
     */
    public interface Developer {
        String makeJob();
    }
